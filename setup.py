# -*- coding: utf-8 -*-

from setuptools import setup

setup(
      name='ftpTransfer',
      version='1.0',
      description='FTP transfer to Cetaris server',
      author='Mark Patmore',
      install_requires=[
          'pyexcel_ods',
          'collections',
          'openpyxl',
          'datetime'
          ]
           
      )

